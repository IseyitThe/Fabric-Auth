plugins {
    id 'java'
}

dependencies {
}

jar {
    manifest {
        attributes(
                'Main-Class': 'me.seyit.socketauthserver.SocketAuthServer',
        )
    }
    archiveFileName = 'server.jar'
}
